// I am not sure if authorization takes a whole middleware separately to handle different kind of client authorization on a web server

function authorization(req,res,next){

}

module.exports = {
    authorization
}